import $ from 'jquery';
import { $WINDOW } from '../helpers';
import ViewBase from '../view_base';

/**
 * グロナビ
 */
export default class extends ViewBase {
  constructor(el) {
    super(el);

  }

  initialize() {
    super.initialize();

  }

  bind() {

  }
}